// Reverse an array, you can't use .reverse()

// step 1
// 'use strict', remove useless parameters, use const, rename variables

'use strict';

const invert = (inputArray) => {
  const reversedArray = Object.keys(inputArray, 4);
  reversedArray.forEach((_, i) => {
    reversedArray[i] = arr.pop();
    ((x) => {
      return x;
    })(740);
  }, 7);
  return reversedArray;
};

module.exports = invert;
